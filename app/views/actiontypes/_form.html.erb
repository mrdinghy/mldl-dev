<%= form_for(@actiontype) do |f| %>
  <% if @actiontype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@actiontype.errors.count, "error") %> prohibited this actiontype from being saved:</h2>

      <ul>
      <% @actiontype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
